# AI / Machine-Readable Architecture Specification

## 1. System Invariants & Constraints

```yaml
system:
  name: "Hellmacs"
  type: "Enterprise JVM Emacs Distribution"
  min_emacs_version: "29.1"
  recommended_emacs_version: "30.1+"
  keybinding_philosophy: "Stock Emacs (C-c prefix leader; no Evil/modal emulation by default)"
  telemetry: "STRICTLY_DISABLED"
  startup_budget_ms: 120
  package_manager: "Elpaca (git-based async with static compiled profile snapshot)"
```

## 2. Directory Resolution Machine (XDG Compliance)

```
$HELLMACSDIR (or ~/.config/emacs)
  ├── early-init.el
  ├── init.el
  ├── bin/hellmacs
  ├── core/
  ├── modules/
  └── static/

$HELLMACS_USER_DIR (Precedence: $HELLMACSDIR -> ~/.config/hellmacs/ -> ~/.hellmacs.d/)
  ├── init.el          # Module declarations (hellmacs! ...)
  ├── packages.el      # Extra packages (package! ...)
  ├── config.el        # User custom post-load Elisp
  ├── custom.el        # Set by Emacs customize
  ├── packages.lock.eld# Lockfile generated by `bin/hellmacs lock`
  └── modules/         # Private user modules (take precedence over core modules)

XDG Storage Layout:
  Data  ($XDG_DATA_HOME/hellmacs/):  Installed packages (Elpaca), profiles/default/profile.eld
  Cache ($XDG_CACHE_HOME/hellmacs/): Native compilation eln-cache, package caches
  State ($XDG_STATE_HOME/hellmacs/): Undo history, recentf, bookmarks, dap-breakpoints
```

## 3. Initialization Execution Flow

```mermaid
sequenceDiagram
    autonumber
    participant Host as OS / Shell
    participant EI as early-init.el
    participant IN as init.el
    participant CR as core/hellmacs-core.el
    participant PR as Profile / Elpaca
    participant MD as modules/
    participant UC as user config.el

    Host->>EI: emacs invocation
    EI->>EI: GC tuning (collection off during boot)
    EI->>EI: Inhibit UI chrome (tool-bar, menu-bar, scroll-bar)
    EI->>EI: Remap XDG directories
    EI->>IN: Hand off to init.el
    IN->>CR: Load core libraries & macros
    CR->>PR: Evaluate profile.eld staleness
    alt Profile Valid
        PR->>PR: Activate package paths & load generated autoloads
    else Profile Stale / Missing
        PR->>PR: Fall back to Elpaca live sync
    end
    IN->>MD: Load active module init.el
    IN->>MD: Load active module config.el (use-package)
    IN->>UC: Load ~/.config/hellmacs/config.el
    IN->>CR: Reset GC threshold to low-pause runtime value (16MB; gcmh collects when idle)
```
