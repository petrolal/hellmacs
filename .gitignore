# Where older Hellmacs versions kept packages and state, before it moved
# to the XDG directories. Nothing writes here any more; safe to delete.
/var/
/etc/

# Byte-compiled / native-compiled artifacts, wherever they land.
*.elc
*.eln
eln-cache/

# Editor/OS cruft.
*~
\#*\#
.DS_Store
